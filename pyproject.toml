[project]
name = "blog-autopost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "feedparser>=6.0.11",
    "pytest>=8.4.1",
    "pytest-cov>=6.2.1",
    "pyyaml>=6.0.2",
    "tweepy>=4.16.0",
]

[project.scripts]
blog-autopost = "src.main:main"

[tool.uv]
