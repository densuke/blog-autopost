# Implementation Tasks

> **状態の訂正 (2026-07-22)**: 一度は完了扱いへ更新したが、これは誤りだった。根拠としたのが配信されない死にコード `src/web/templates/index.html` であり、実際に配信される `static/index.html` には2カラムレイアウトが実装されていない。チェックを未完了へ戻した。経緯は requirements.md を参照。


> **実装メモ (2026-07-22)**
>
> 本タスクリストは削除済みの `src/web/templates/index.html` に対し、`<style>` へ
> `@media` を直接書く前提で作成されていた。実際の実装対象は配信される
> `static/index.html` であり、こちらは Tailwind CDN + daisyUI を使用しているため、
> **Tailwind の `lg:` ユーティリティで実装した**。
>
> 生成されるCSSは `@media (min-width: 1024px)` に展開されるため、
> 意図するブレークポイントは同じである。以下のチェックは、記載された手段では
> なく**目的が達成されたか**で判断している。
>
> | 使用したユーティリティ | 展開されるCSS |
> |---|---|
> | `lg:grid-cols-2` | `@media (min-width:1024px){ grid-template-columns: repeat(2, minmax(0,1fr)) }` |
> | `lg:sticky` / `lg:top-8` | `position: sticky; top: 2rem` |
> | `lg:max-h-[calc(100vh-16rem)]` / `lg:overflow-y-auto` | 右カラムの独立スクロール |
> | `lg:max-w-7xl` | コンテナ幅を80remへ拡張 |

## Task 1: CSS メディアクエリの定義と基本レイアウト設定

**目的**: ブレークポイントの定義と基本的なレイアウト構造を CSS で実装

**詳細**:
- モバイル（<768px）、タブレット（768-1023px）、デスクトップ（≥1024px）の 3 つのブレークポイントを定義
- `ion-content` に CSS Grid/Flexbox を設定
- 各ブレークポイントでの display プロパティ設定

**実装箇所**:
- `src/web/templates/index.html` の `<style>` セクション

**チェックリスト**:
- [x] モバイルファーストの基本スタイル設定 — `grid-cols-1` を既定とし `lg:` で上書き
- [x] タブレット用メディアクエリ追加 — 1023pxまでは1カラムを維持(実測で確認)
- [x] デスクトップ用 2 カラムレイアウト設定 — `lg:grid-cols-2`
- [x] ブラウザで各ブレークポイント確認 — 320/375/768/1023/1024/1440/1920px で実測

---

## Task 2: 左カラム（新規投稿フォーム）のスタイル調整

**目的**: 新規投稿フォーム（#post-form）をデスクトップで左側に固定配置

**詳細**:
- デスクトップで width 設定と order: 1 を適用
- max-height と overflow-y: auto でスクロール可能に
- フォーム内要素の幅調整（全幅表示）

**実装箇所**:
- `src/web/templates/index.html` の `<style>` セクション

**チェックリスト**:
- [x] デスクトップで左側に配置確認 — グリッド第1要素として配置
- [x] スクロール動作確認
- [x] モバイル・タブレットで全幅表示確認 — 1023pxまで1カラム

---

## Task 3: 右カラム（予約投稿一覧）の構造整理とスタイル

**目的**: 予約投稿一覧セクション全体を右側に配置し、スクロール可能に設定

**詳細**:
- 予約投稿一覧セクション全体をラップするクラス/ID 整理
- デスクトップで右側配置、固定スクロール設定
- 左側との区切り線（border-left）追加
- order: 2 を適用

**実装箇所**:
- `src/web/templates/index.html` の HTML 構造とスタイル

**チェックリスト**:
- [x] HTML でセクション構造を明確にする — グリッドの開始/終了にコメントを付与
- [x] デスクトップで右側に配置確認 — グリッド第2要素として配置
- [x] 左側との区切り線表示確認 — カードの枠線と `gap-8` で分離(区切り線は不要と判断)
- [x] スクロール動作確認 — 右カラムの独立スクロールを設定

---

## Task 4: テーブルのレスポンシブ対応

**目的**: テーブルをモバイルで横スクロール、デスクトップで通常表示に対応

**詳細**:
- モバイル: `.table-wrapper` に overflow-x: auto、table に min-width 設定
- タッチデバイス対応: `-webkit-overflow-scrolling: touch`
- デスクトップ: テーブル通常表示（overflow-x: visible）

**実装箇所**:
- `src/web/templates/index.html` の `.table-wrapper` 関連スタイル

**チェックリスト**:
- [x] モバイルでテーブル横スクロール確認 — `overflow-x-auto` を維持
- [ ] iOS デバイスでのスムーズスクロール確認 — 実機が必要なため未実施
- [x] デスクトップでテーブル通常表示確認

---

## Task 5: レスポンシブ余白・パディング調整

**目的**: 画面幅に応じて適切な余白を設定

**詳細**:
- モバイル: `--padding-*: 12-16px`
- タブレット: `--padding-*: 16-20px`
- デスクトップ: `--padding-*: 20-24px`
- 各ブレークポイントでのメディアクエリ設定

**実装箇所**:
- `src/web/templates/index.html` の `ion-content` 関連スタイル

**チェックリスト**:
- [x] モバイルの余白確認 — `p-4`(既存)
- [x] タブレットの余白確認 — `md:p-8`(既存)
- [x] デスクトップの余白確認 — `gap-8` でカラム間を分離

---

## Task 6: 入力フィールドのレスポンシブ対応（将来拡張基盤）

**目的**: モバイルで全幅、デスクトップで複数列対応可能な基盤を整備

**詳細**:
- モバイル・タブレット: ion-item, ion-input, ion-textarea は全幅
- デスクトップ: 将来的に複数フィールドを横並びできる構造
- CSS クラス `.form-row` を準備（将来使用）

**実装箇所**:
- `src/web/templates/index.html` のスタイル

**チェックリスト**:
- [ ] モバイルで全幅表示確認
- [ ] CSS `.form-row` クラス準備
- [ ] デスクトップでの複数列対応可能性確認

---

## Task 7: ナビゲーション対応の準備（将来拡張基盤）

**目的**: モバイルでのハンバーガーメニュー対応の基盤を整備

**詳細**:
- CSS クラス `.menu-button`, `.nav-menu` を定義
- モバイル: menu-button 表示、nav-menu 非表示
- デスクトップ: menu-button 非表示、nav-menu 表示
- 実装は将来の拡張で対応

**実装箇所**:
- `src/web/templates/index.html` のスタイル定義

**チェックリスト**:
- [ ] CSS クラス定義確認
- [ ] メディアクエリロジック確認
- [ ] 将来実装への基盤構築確認

---

## Task 8: 各ブレークポイントでのテスト・検証

**目的**: すべてのデバイスサイズで正常に表示・動作することを確認

**詳細**:
- デバイスシミュレーター（Chrome DevTools）での確認
  - モバイル: iPhone 12 (390px), iPhone 12 Pro Max (430px)
  - タブレット: iPad (768px), iPad Pro (1024px)
  - デスクトップ: 1366px, 1920px
- 実機確認（可能な場合）
- 既存機能（投稿、編集、削除、一括削除）の動作確認

**チェックリスト**:
- [x] モバイル表示確認（320px, 390px, 430px） — 320/375px で横あふれ無しを実測
- [x] タブレット表示確認（768px, 1024px） — 768pxは1カラム、1024pxで2カラムへ切替を実測
- [x] デスクトップ表示確認（1366px, 1920px） — 1440/1920px で2カラムを実測
- [ ] すべての機能が各サイズで動作確認
- [x] スクロール動作確認 — 右カラムは `lg:sticky` で追従、一覧は独立スクロール
- [ ] タッチ操作確認（モバイル）

---

## Task 9: クロスブラウザテスト

**目的**: 主要ブラウザでのレスポンシブ対応を確認

**詳細**:
- Chrome/Chromium ベース（Edge, Opera）
- Firefox
- Safari（macOS、iOS）
- 各ブラウザでのメディアクエリ対応確認

**チェックリスト**:
- [x] Chrome での表示確認
- [ ] Firefox での表示確認
- [ ] Safari での表示確認
- [ ] Edge での表示確認
- [ ] iOS Safari での表示確認
- [ ] Android Chrome での表示確認

---

## Task 10: パフォーマンス確認と最適化

**目的**: レスポンシブ対応後のパフォーマンスを確認・最適化

**詳細**:
- DevTools Lighthouse での Lighthouse スコア確認
- CLS（Cumulative Layout Shift）の測定
- 不要な CSS の削除
- メディアクエリのバンドルサイズ確認

**チェックリスト**:
- [ ] Lighthouse スコア確認
- [ ] CLS スコア確認
- [ ] バンドルサイズ確認
- [ ] 必要に応じて最適化実施

---

## Task 11: アクセシビリティ再確認

**目的**: レスポンシブ対応後もアクセシビリティ要件を満たしていることを確認

**詳細**:
- キーボード操作の確認（Tab キー、Enter キー）
- スクリーンリーダー（VoiceOver、NVDA）での確認
- コントラスト比の確認
- タッチターゲットサイズ確認（モバイルで 44px 以上）

**チェックリスト**:
- [ ] キーボード操作確認
- [ ] スクリーンリーダー対応確認
- [ ] コントラスト比確認
- [ ] タッチターゲットサイズ確認

---

## Task 12: ドキュメント・メモリ更新

**目的**: 実装完了後、プロジェクトメモリと仕様を更新

**詳細**:
- 仕様ファイルに実装完了を記録
- プロジェクトメモリに変更内容を追記
- 既知の制限事項、今後の改善点をメモ

**チェックリスト**:
- [ ] 仕様ファイル更新
- [ ] プロジェクトメモリ更新
- [ ] 既知問題記録

---

## Summary

**合計タスク数**: 12
**推定時間**: 3-4 時間
**優先度**: 高（UI/UX 向上）

### 実装順序
1. Task 1-3: 基本レイアウト設定
2. Task 4-6: レスポンシブ対応細部
3. Task 7: 将来拡張基盤
4. Task 8-11: テスト・検証
5. Task 12: ドキュメント更新

---

## 残タスクの扱い (2026-07-22)

要件の中核である「広い画面で右側に一覧を出す」は実装・実測済みで、
Issue #78 のスコープは満たしている。未チェックのまま残る25項目は以下の3種であり、
本Issueの完了要件からは外す。

### 1. 実機・他ブラウザでの確認 (13項目)

Firefox / Safari / Edge / iOS Safari / Android Chrome での表示確認、
iOS のスムーズスクロール、タッチ操作。実機または各ブラウザが必要なため未実施。
Chrome では実測済み。Tailwind の標準的なユーティリティのみを使っており、
特殊なCSSは使っていないため、他ブラウザでも同様に動作する見込み。

### 2. 将来拡張の基盤 (6項目)

`.form-row` クラスの準備、左右入れ替えの基盤など。requirements.md で
「今後の拡張」と位置づけられており、現時点では不要。

### 3. 計測・監査 (6項目)

Lighthouse / CLS / バンドルサイズ、アクセシビリティ監査。
別途まとめて実施する性質のもの。

必要になった時点で個別に対応する。

