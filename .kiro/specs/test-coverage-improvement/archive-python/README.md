# アーカイブ: Python版 test-coverage-improvement

このディレクトリのファイルは、Rust移植前のPython実装を前提に書かれた旧specです。
`pytest` / `ScheduledPostStoreSQLite` / カバレッジ49%といった記述は現行の `main`
(Rust版) には適用できません。

履歴として保存しているだけであり、参照先としては使わないでください。
現行のspecは1つ上の階層の `requirements.md` / `design.md` / `tasks.md` です。

退避日: 2026-07-22 (Issue #59)
